## 1. Исходное состояние

- [x] 1.1 Сохранить исходный срез и разбор ERROR; измерить исходную загрузку/генерацию Paddle на синтетическом
  изображении в отдельном evidence.

## 2. Общий OCR

- [x] 2.1 Ввести общие профили и CLI-параметры; тесты проверяют defaults, пути, несовпадение профиля и недопустимые
  значения.
- [x] 2.2 Оптимизировать загрузчик и pool ёмкости 1; тесты проверяют устройство, INT8, CPU fallback, повторное
  использование, смену и ошибку.
- [x] 2.3 Передать размер изображения и включить KV-кеш/детерминированный inference; проверить регрессии min_pixels,
  пустого ответа, метрики и предупреждение о пределе.
- [x] 2.4 Разделить кеш по модели/параметрам и защитить старые TXT; тесты проверяют совпадение до модели и промах после
  изменения параметров.

## 3. Веб

- [x] 3.1 Добавить каталог/API и lazy-загрузку; API/SSE-тесты проверяют выбор, отсутствие preload, изоляцию результатов
  и освобождение слота после отказа.
- [x] 3.2 Добавить выбор/параметры/метрики в UI; браузер проверяет смену defaults, редактирование, upload и завершение
  OCR.

## 4. Приёмка и архив

- [x] 4.1 Выполнить общий pytest, pip check, сборку и доступные проверки JS; сохранить итог с фактическим охватом.
- [x] 4.2 Выполнить реальные GLM/Paddle на синтетических входах и сопоставимое измерение Paddle до/после; сохранить
  тексты, параметры, времена и ограничения качества.
- [x] 4.3 Синхронизировать README/CHANGELOG/контекст/ADR и основные specs; проверить ссылки, UTF-8 и openspec validate;
  архивировать только завершённое изменение.

Точка продолжения: [context-summary](evidence/context-summary.md). Доказательства: [evidence](evidence/).
